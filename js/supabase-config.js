// Supabase project config — the anon key is public by design (RLS protects
// all data; writes to profiles/devices happen only via the service role).
//
// TODO: fill in after creating the Supabase project. These values must match
// SUPABASE_URL / SUPABASE_ANON_KEY in the Vercel env, and src/config/license.ts
// in the extension.
window.JUTTR_SUPABASE = {
  url: '',
  anonKey: '',
};
